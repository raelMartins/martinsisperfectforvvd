/** Programmatic auto-scroll pacing for the scrollytelling timeline. */
export const AUTO_SCROLL = {
  /** Steady downward speed in CSS pixels per second. */
  pixelsPerSecond: 52,
  /** Brief pause after load before scrolling begins. */
  startDelayMs: 500,
} as const;
